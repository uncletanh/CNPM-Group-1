# Dùng khi chạy `npm run dev` cục bộ (widget nhúng thật lấy config từ data-* trên thẻ <script>).
# Copy file này thành .env.local rồi điền workspace_id + widget_token thật (xem trong Dashboard > Workspace).
VITE_API_URL=http://localhost:8000/api/v1
VITE_WORKSPACE_ID=1
VITE_WIDGET_TOKEN=
